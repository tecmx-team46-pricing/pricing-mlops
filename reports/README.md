# Reports

This directory is for small, reviewed report templates or examples.

Generated run reports belong under `runs/local/<run_id>/` locally or in the platform-managed `reports` container in Azure. Do not commit generated reports from real or masked production-like runs unless explicitly reviewed and sanitized.
